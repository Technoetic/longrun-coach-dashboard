# Step 44 - 디버깅

step046_테스트결과_chunk*.md에서 발견된 오류를 수정한다.

## c8 커버리지 활용 디버깅

`npx c8 report --reporter=html` 로 HTML 커버리지 리포트를 생성하여, 테스트가 도달하지 못한 라인/브랜치를 시각적으로 확인한다. 미실행 경로가 실패 원인과 관련될 수 있으므로 이를 참고하여 디버깅한다.

합리적인 선에서 최대한 많은 서브에이전트를 병렬로 사용하여 디버깅한다.

**디버깅 단계에서 절대로 plan mode를 사용하지 않는다.**

서브에이전트는 항상 haiku를 사용한다.

---

이 지침을 완료한 즉시 자동으로 step045.md를 읽고 수행한다. 사용자 확인을 기다리지 않는다.
