# Step 16 - 참고 레포지토리 클론 및 코드 분석

Step 15 조사결과에서 발견한 참고 레포지토리를 클론하고, 핵심 패턴을 실제 코드로 확인한다.

## 절차

1. `.claude/step015_조사결과_chunk*.md` 파일들을 읽어 참고할 레포지토리 URL 목록을 추출한다
2. `.claude/references/` 디렉토리에 shallow clone (`--depth 1`)으로 클론한다
3. 클론한 레포에서 다음을 분석한다:
   - 디렉토리 구조 (`tree` 또는 `ls -R`)
   - 핵심 설정 파일 (package.json, tsconfig.json, 빌드 설정 등)
   - 주요 진입점 및 라우팅 패턴
   - 컴포넌트/모듈 구조
   - 상태 관리 방식
4. 분석 결과를 청크 단위로 기록한다

## 규칙

- **shallow clone만 사용** (`git clone --depth 1`) — 용량 절약
- 클론 대상 디렉토리: `.claude/references/<repo-name>/`
- 이미 클론된 레포는 스킵
- 클론 실패 시 에러 기록 후 다음 레포로 진행
- 최대 5개 레포까지만 클론 (가장 관련도 높은 순)
- 분석 결과는 이후 설계/구현 Step에서 직접 참조할 수 있도록 구체적으로 작성

**분석 결과는 청크 단위로 저장한다:**

```
step017_조사결과_chunk1.md (500줄 이하)
step017_조사결과_chunk2.md (500줄 이하)
step017_조사결과_chunk3.md (500줄 이하)
...
```

**작성 규칙**:
- 각 청크는 500줄 이하로 작성 (성능 최적화)
- `.claude/hooks/research-validator.ps1`에서 각 청크 검증 (BOM/CRLF/줄수/파일크기)
- 청크 그대로 유지 (병합 안 함)

## 분석 결과 파일 형식

```markdown
# Step 16 - 참고 레포 코드 분석 (chunk N)

## 클론 현황 (chunk1에만 포함)
| 레포 | 경로 | 상태 |
|------|------|------|
| owner/repo | .claude/references/repo/ | 성공/실패 |

## 레포별 분석

### 1. owner/repo
- **디렉토리 구조**: ...
- **기술 스택**: ...
- **핵심 패턴**: ...
- **우리 프로젝트에 적용할 점**: ...
```

서브에이전트는 항상 haiku를 사용한다.

---

이 지침을 완료한 즉시 자동으로 step017.md를 읽고 수행한다. 사용자 확인을 기다리지 않는다.
